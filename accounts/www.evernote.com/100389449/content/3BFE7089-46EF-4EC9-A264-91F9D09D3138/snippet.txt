# Written by KFH: karshi.hasanov@utoronto.ca # Date: November 10, 2010 # Last Modified: August 19, 2017 # Description: Installing the custom FreeBSD kernel # VMware Fusion (ver 8.5.8 ) on Mac OSX ( ver 10.11.6) # VM FreeBSD - 11.1 The Custom Kernel Installation # Require : The full FreeBSD source tree => ls /usr/src ( if empty, you don’t have the FreeBSD sources installed) # Find out your architecture: => uname -a … GENERIC amd64 # Change pwd to the location of the kernel configurations folder => cd /usr/src/sys/amd64/conf # Copy the default kernel GENERIC to your custom kernel => sudo cp GENERIC KFH # Edit the custom kernel (KFH) and change to your needs. # Read the /usr/src/sys/amd64/conf/NOTES for more details # Read my notes below “How Do I know all the installed Hardware?" # IMPORTANT: Don’t forget to change the name of the custom kernel. ident KFH # Building the custom kernel => cd /usr/src => sudo make buildkernel KERNCONF=KFH # Install the new kernel associated with the specifi