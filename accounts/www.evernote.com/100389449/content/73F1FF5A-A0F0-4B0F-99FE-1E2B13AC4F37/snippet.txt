#: Website: http://hub.docker.com #: User: khasanov #: Password: OLDBASE #: Email: UofT #: Repository Name : kfh-images Docker Basics # If needed: # => docker login # khasanov/OldBase # How to search an image => docker search ubuntu # To download an image: => docker pull ubuntu # same as => docker pull ubuntu:latest # How to run the image: => docker run -ti --name=kfh-ubuntu ubuntu /bin/bash # To get back press “Control + P+Q” # To see how many containers are currently running in your system: => docker ps # To stop a container => docker stop < CONTAINER ID> or <NAMES> => docker stop db8d0869540f # or hopeful_wilson # This will start without attacing ( no shell promt) # To attach the container after the starting “-d” option (deattach mode): => docker attach db8d0869540f Removing Docker Containers # “-q” prints only the container ID’s # “-a” prints all containers # To see all running containers: => docker ps -a # To remove a single container: => docker rm <ContainerID> => docker rm d7ad4