Admin Tools How to configure sysstat on Debian “”” The statistics reported by sar deal with I/O transfer rates, paging activity, process-related activities, interrupts, network activity, memory and swap space utilization, CPU utilization, kernel activities and TTY statistics, among others. Both UP and SMP machines are fully supported. SAR = System Active Reoprted “”" => sudo apt-get install sysstat # Edit “/etc/default/sysstat and set ENABLE = “true" => sudo vim /etc/default/sysstat # You can change the collection interval by editing /etc/cron.d/sysstat => sudo vim /etc/cron.d/sysstat # Restart the sysstat => sudo service sysstat restart # If you want to see all statistics : => sar -A 