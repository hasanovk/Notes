Step1 : Install the OpenSSL using the brew => brew install openssl Step 2: Download the Python source code ( such as Python-3.6.1.tgz ) and extract Step 3: Change directory to the Python source folder and open the Modules/Setup.dist” and Modules/Setup files Step 4. Search for “SSL” ( somewhere near line 204-209) and uncomment the lines: _socket socketmodule.c # Make sure that “SSL” is set to the OpenSSL installation directory! SSL=/usr/local/opt/openssl _ssl _ssl.c \ -DUSE_SSL -I$(SSL)/include -I$(SSL)/include/openssl \ -L$(SSL)/lib -lssl -lcrypto 