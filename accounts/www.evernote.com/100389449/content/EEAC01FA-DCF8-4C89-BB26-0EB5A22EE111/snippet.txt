Installing from the Ports Tree # Always update the ports treee => sudo portsnap fetch update # The Webmin ports location => cd /usr/ports/sysutils/webmin => sudo make install clean After installing Webmin for the first time you should perform the following steps as root: * Configure Webmin by running /usr/local/lib/webmin/setup.sh * Add webmin_enable="YES" to your /etc/rc.conf * Start Webmin for the first time by running "service webmin start" The parameters requested by setup.sh may then be changed from within Webmin itself. # Configuring the Webmin # Note: The Webmin uses its own webserver and it supports SSL if only if the system has # the “Perl SSLeay library” installed. So, don’t look in the Apache Server configuration for ot! => sudo /usr/local/lib/webmin/setup.sh Log file directory [/var/log/webmin]: <Entrer> Full path to perl (default /usr/local/bin/perl): <Enter> Web server port (default 10000): 2017 Login name (default admin): khasanov Login password: Hydrogen***3 Password ag