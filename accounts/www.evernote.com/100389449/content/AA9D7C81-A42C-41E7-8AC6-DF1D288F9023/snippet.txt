Installing the Required Packages => sudp apt-get update => sudo apt-get install adb => sudo apt-get install abootimg => sudo apt-get install fastboot Accessing the Android Device # I am using the VM Ubuntu 16.04 on the VBox # The USB Port need to accept the connection from the device. # So, after connecting the device go to the “Settings” -> “Ports” -> “USB” # and add (+) . You should be able to choose the “Android [xxxx]”. # Disconnect and reconnect the device => adb devices List of devices attached e72fb810 no permissions # Use Ubuntu only to unpack, modify and pack files after you have # copied to a local folder. # It won’t have the root privileges to talk to the device. # Things like install/remove, backup and dealing with the file systems # use the MAC OS X Terminal. 