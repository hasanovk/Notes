FreeBSD Installation (ver 11.0 64 bit) VMware Fusion Professional: Version 8.5.8 I've installed the ZFS file system instead of UFS After the installation, the system and packages need to update: => sudo /usr/sbin/freebsd-update fetch => sudo /usr/sbin/freebsd-update install => sudo /usr/sbin/portsnap fetch => sudo /usr/sbin/portsnap extract # Disable Sendmail in "/etc/rc.conf" sendmail_enable="NO" sendmail_submit_enable="NO" sendmail_outbound_enable="NO" sendmail_msp_queue_enable="NO" Clock Synchronization with NTP Add the line ntpd_enable=“YES” in your /etc/rc.conf file Add the following lines in /etc/ntp.conf file: server 0.ca.pool.ntp.org prefer server 1.ca.pool.ntp.org iburst server 2.ca.pool.ntp.org iburst # The drift file is located under /var/db/ntpd.drift by default # To update the time => sudo ntpdate -v -b 0.ca.pool.ntp.org Installing the VMware Tools Step 1: => sudo pkg install -y perl5 Step 2: => sudo pkg install -y compat6x-amd64 Step 3: “Virtual Machine” -> “Install VM To