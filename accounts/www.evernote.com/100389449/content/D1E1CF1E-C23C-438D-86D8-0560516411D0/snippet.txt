FAQ # How to check what version of Debian I am running? => lsb_realese -a # No “ifconfig” found: => sudo apt-get install -y net-tools # I don’t want to use the CD/DVD => sudo vi /etc/apt/sources.list # Disable the CD # How do I autologin to the KDE 5 Plasma ? => su => sddm --example-config > /etc/sddm.conf # Edit the "sddm.conf” file [Autologin] User=khasanov Session=plasma.desktop # How do I make shortcuts in the KConsole” “Settings" -> “Configure Shortcuts…” ( Note: If you are using the Apple keyboard, the CMD might not work) 