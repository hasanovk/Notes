Installing the Java JDK8 ( Open JDK8) => cd /usr/ports/java/openjdk8 => export BATCH=1 => sudo make -DBATCH install clean ( I didn’t have to these for Java 8 ) Edit the "/etc/fstab" and add the lines: fdesc /dev/fd fdescfs rw 0 0 proc /proc procfs rw 0 0 => sudo mount -a => sudo ln -s /usr/local/openjdk8 /usr/local/java ENVIRONMENT VARIABLES: JAVA_HOME = /usr/local/openjdk8 JAVAC = ${JAVA_HOME}/bin/javac JAVA = ${JAVA_HOME}/bin/java ( no space between !!!) 