Installing the Node.js # Version: 10.3 # Update the Ports Tree => cd /usr/ports/www/node => sudo make install clean # The above command forced me to edit the “/etc/make.conf” and add the line DEFAULT_VERSIONS+=ssl=openssl-devel # Then run the “make install clean” command again # It will install the "openssl-devel-1.1.0” package as a dependency first. In intalling the NPM (Node Package Manager) # Version: npm-5.10.0 => cd /usr/ports/www/npm 