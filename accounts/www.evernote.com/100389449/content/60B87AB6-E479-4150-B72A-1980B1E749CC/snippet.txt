Installing Pyramid on FreeBSD # Website: https://trypyramid.com/ => mkdir -p /opt/Projects/Pyramid => cd /opt/Projects # Setting up the virtual python environment => python3 -m venv Pyramid => cd Pyramid => bin/pip install "pyramid==1.9.1” # We will be using the Template Jinja2, so lets install it as well. => bin/pip install Jinja2 Creating First Pyramid Application => mkdir /opt/Projects/Pyramid/HelloWorld => cd /opt/Projects/Pyramid/HelloWorld # Create the following file: # Name: helloworld.py from wsgiref.simple_server import make_server from pyramid.config import Configurator from pyramid.response import Response def hello_world(request): return Response('Hello %(name)s!' % request.matchdict) if __name__ == '__main__': with Configurator() as config: config.add_route('hello', '/hello/{name}') config.add_view(hello_world, route_name='hello') app = config.make_wsgi_app() server = make_server('192.168.100.150', 8585, app) server.serve_forever() => ../bin/python helloworld.py & => socks