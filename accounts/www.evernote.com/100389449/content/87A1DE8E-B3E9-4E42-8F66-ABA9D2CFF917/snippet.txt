# Written by KFH: karshi.hasanov@utoronto.ca # Date: October 5, 2015 # Last Modified: August 8, 2017 # Description: Installing and setting up the DNS Server on FreeBSD 11.1 Installation Note: * Since FreeBSD 10.x no more the “base” BIND in the FreeBSD So, you won’t find "/etc/namedb” and "/usr/sbin/named” and you don’t need to make a link to those files. => cd /usr/ports/dns/bind99 = sudo make install clean Add the line named_enable="YES" to "/etc/rc.conf" Make sure the following files are setup "/etc/hosts" and "/etc/resolv.conf" ⁃-------------------- /etc/hosts ------------------- 127.0.0.1 localhost 192.168.100.150 freebsd.karshi.ca vmfreebsd -------------------- End --------------------------- Note: You can only put maximum 3 nameservers in the "resolve.conf" file. We put "192.168.100.150" and "192.168.100.1" in a case if our DNS Server will stop working. Otherwise, we don't need them. --------------------/etc/resolv.conf--------------- search karshi.ca nameserver 127.0.0.1 nameser