# Written by KFH: karshi.hasanov@utoronto.ca # Date: October 5, 2015 # Description: Installing and setting up the DNS Server on FreeBSD 11.1 # Last Modified: March 18, 2018 # Updates: 1. The “/etc/hosts” and “/etc/resolv.conf” files modified. # You only need the localhost (i.e. “127.0.0.1”) to be present. # 2. The second zone , 172.16.0.0/24, was added and tested. Installation Note: Since FreeBSD 10.x no more the “base” BIND in the FreeBSD So, you won’t find "/etc/namedb” and "/usr/sbin/named” and you don’t need to make a link to those files. => cd /usr/ports/dns/bind99 = sudo make install clean Add the line named_enable="YES" to "/etc/rc.conf" Make sure the following files are setup "/etc/hosts" and "/etc/resolv.conf" ⁃-------------------- /etc/hosts ------------------- 127.0.0.1 localhost --------------------/etc/resolv.conf--------------- search karshi.ca nameserver 127.0.0.1 # You are allowed 3 nameservers in the "resolve.conf" file. # The order is important: The first DNS gets cal