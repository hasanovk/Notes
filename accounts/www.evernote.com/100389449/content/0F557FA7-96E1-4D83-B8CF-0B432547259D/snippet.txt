Installing Ubuntu under FreeBSD # Packages Require: vm-bhyve and grub2-bhyve # => sudo pkg install vm-bhyve grub2-bhyve # or => cd /usr/ports/sysutils/vm-bhyve/ => sudo make install clean => cd /usr/ports/sysutils/grub2-bhyve/ => sudo make install clean # Load kernel modules => sudo kldload if_bridge if_tap nmdm vmm # Make loading of kernel modules persistent # Edit the /boot/loader.conf and add the following line: if_bridge_load=“YES" if_tap_load=“YES" nmdm_load=“YES" vmm_load=“YES" # Edit the /etc/rc.conf and add the lines vm_enable=“YES" vm_enable=“YES" vm_list=“" vm_delay=“5" # Note. The above step can be done using the “sysrc” command: # sysrc vm_enable=“YES” # Create a zfs file system for VMs => sudo zfs create -o mountpoint=/vms zroot/vms # Note: My zpool name is “zroot”. Replace it with yours # Initiate the VM => sudo vm init # Copy VM Template examples to the /vms/.templates/ => sudo /usr/local/share/examples/vm-bhyve/* /vms/.templates/ # You will find different template examp