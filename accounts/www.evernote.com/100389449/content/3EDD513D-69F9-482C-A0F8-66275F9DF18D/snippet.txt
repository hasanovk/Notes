SAMBASamba Server Installation => cd /usr/ports/net/samba36 && sudo make install Hint: If you have tried previously and decided fresh config: => sudo make rmconfig ( this will remove the old config file Enable: ACL_SUPPORT AIO_SUPPORT ( Asyncronous IO support ) CUPS ( CUPS printing support ) PORT ( Sytem wide PORT library ) PTHREADPOOL ( Pthread pool ) SWAT ( SWAT WebGUI ) WINBIND (WinBIND support ) Edit "/etc/rc.conf" and add the line samba_enable = "Yes" Web Interface GUI ( SWAT ) setup: If you have enabled the "SWAT" option as above configuration: 1.Uncomment the following line in the "/etc/inetd.conf" file 1.stream tcpnowait/400root/usr/local/sbin/swatswat 1.If you already have running the "inetd": 1.=> sudo killall -HUP inetd ( to restart the inetd ) b. If not, then you need to add the line inetd_enable="YES" in the "/etc/rc.conf" file, then => sudo /etc/rc.d/inetd onestart 2.If everything's ok, then you can locally have access to http://localhost:901 3.In order a user to use the 